# Plantilla de variables de entorno
#
# Vite carga el archivo .env segun el modo del comando:
#
#   npm run dev       -> .env.development
#   npm run build:dev -> .env.staging
#   npm run build     -> .env.production
#
# Copiar este archivo con el nombre correspondiente y ajustar VITE_API_URL.
# Vite incrusta (inlinea) VITE_API_URL en el bundle al momento del build;
# se puede verificar buscando la URL en dist/assets/*.js

# URL base del backend
VITE_API_URL=https://148.234.2.210:30704/api/v1
